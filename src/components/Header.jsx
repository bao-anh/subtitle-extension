import React from 'react';
import { Divider } from 'antd';

const Header = () => (
  <Divider
    className="header-divider"
    orientation="center"
  >
    Inject subtitle
  </Divider>
);

export default Header;
